# Stock Factor Analysis Dashboard

Starter repo for a Streamlit dashboard that analyses stock returns, computes risk metrics and runs PCA.

See `src/` for scripts and `app/streamlit_app.py` for the app.
